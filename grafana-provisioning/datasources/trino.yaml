apiVersion: 1

datasources:
  - name: Trino
    type: trino-datasource
    access: proxy
    url: http://trino:8080
    user: airflow
    jsonData:
      catalog: iceberg
      schema: edge_device
    isDefault: false
    editable: true
